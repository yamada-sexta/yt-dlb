// Source: test/test_age_restriction.py

import { describe, test } from "bun:test";

describe("age restriction downloads", () => {
  test.todo("TestAgeRestriction.test_youtube once age-limit extraction and infojson writing are ported", () =>
    undefined);
  test.todo("TestAgeRestriction.test_youporn once YouPorn extraction and age-limit filtering are ported", () =>
    undefined);
});
