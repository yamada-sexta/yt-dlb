// Source: test/test_config.py

import { describe, test } from "bun:test";

describe("configuration file loading", () => {
  test.todo("TestConfig.test_config__ENVIRON_DEFAULTS_sanity once config file discovery is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_all_environ_values once config file discovery is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_default_expected_locations once config file discovery is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_default_grouping once config file discovery is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_early_exit_commandline once --ignore-config file handling is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_early_exit_files once --ignore-config file handling is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_override_commandline once config precedence is ported", () =>
    undefined);
  test.todo("TestConfig.test_config_override_files once config precedence is ported", () =>
    undefined);
});
